# The dev server proxies /api and websockets to the backend (see vite.config.ts).
# For production builds pointing at a remote backend, set this and use it in api/client.ts:
# VITE_API_URL=https://your-backend.example.com
